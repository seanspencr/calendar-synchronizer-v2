In dashboard page :
1. remove toggling btween month and week
2. add 2 column below the calendar, left one 'Daily QUest' and right one is 'All Tasks'. Both renders things like tasklist, the tasks form right column (all task) is able to be dragged into the left one (daily quest), which means I the user is commiting to do that task today.. Reverse action is also possibl e(dragging from left to right means user is uncommiting to do that task today)

please prioritize reusing the components that already built in the dashboard page (TaskItem for example)

please prioritize utilizing tamagui components as much as possible.

please use dummy hooks for data like : 
- get profile detail

please use dtos generated  by the openapi client in generated/openapi/dto to simulate the data in the frontend

MAKE SURE THE CODE IS READABLE AND MAINTAINABLE WITH ATOMIC COMPONENTS
